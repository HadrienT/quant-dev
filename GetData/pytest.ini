[pytest]
testpaths = tests
markers =
    network: hits a live external service (yfinance); deselect with -m "not network"
    postgres: needs a reachable Postgres; skipped automatically when there is none
