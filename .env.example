# Copy to .env and fill in. .env is git-ignored.
PGDATABASE=quantdev
PGUSER=quantdev
PGPASSWORD=change-me

# Only needed by the Streamlit app. Use a freshly rotated key: the previous one
# was exposed by the tf.plan file committed to this repository's history.
FRED_API_KEY=

LOG_LEVEL=INFO
